# URL of the deployed Google Apps Script Web App (see google-script/code.gs).
# Copy this file to `.env` and fill in the value, or leave it unset and the
# kiosk will prompt for the URL on first launch (saved to localStorage).
VITE_APPS_SCRIPT_URL=https://script.google.com/macros/s/AKfycb.../exec
